void main() {
// Declaring Variables
double prize = 1110.22323422231; // valid.
print(prize.toStringAsFixed(3));
}

/**
 * The .toStringAsFixed(3) is used to round the double value upto 3 decimal places in dart. 
 * We can round to any decimal places by entering numbers like 1, 2, 9, etc.
 */